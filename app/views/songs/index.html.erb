<% @songs.each do |song| %>
<p><%= song.artist_name + " - " + song.name %></p>
<% end %>


  it "lists songs in 'Artist Name - Song Title' format" do
    visit songs_path
    expect(page).to have_link("Daft Punk - The Grid", href: song_path(@song))
  end

